//! Benchmark client — a real client of the same socket as any other order
//! entry connection. Stage 0 is a stub; the HDR-histogram burst-workload
//! client is built in stage 7 (SPEC §9).

fn main() {
    println!("bench: stage 0 scaffold, no client wired yet");
}
