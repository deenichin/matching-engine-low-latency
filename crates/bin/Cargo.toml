[package]
name = "bin"
version.workspace = true
edition.workspace = true

[[bin]]
name = "engine"
path = "src/main.rs"

[dependencies]
core = { path = "../core" }
gateway = { path = "../gateway" }
risk = { path = "../risk" }
