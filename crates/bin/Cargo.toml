[package]
name = "bin"
version.workspace = true
edition.workspace = true

[[bin]]
name = "engine"
path = "src/main.rs"

[dependencies]
core = { path = "../core" }
gateway = { path = "../gateway" }
marketdata = { path = "../marketdata" }
risk = { path = "../risk" }

[dev-dependencies]
wire = { path = "../wire" }
