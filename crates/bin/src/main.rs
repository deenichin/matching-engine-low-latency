//! The daemon. Wires `gateway`, `risk`, `marketdata`, and `core` together.
//! Built in stage 3 onward (SPEC §4); stage 0 is a stub so the workspace
//! and Docker image both have something to build and run.

fn main() {
    println!("matching-engine: stage 0 scaffold, no transport wired yet");
}
